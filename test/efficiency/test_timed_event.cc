int main(void)
{
        /*
        TimedEvent t;
        srand(time(NULL));
        CClock::UpdateTime();

        printf("now = %s\n", CClock::GetTimeToString().c_str());
        uint64_t now = CClock::GetTimeStamp();
        TimedEventData* data = new TimedEventData(now+2, 3, -1, []()
                        {
                                printf("timed now = %s", CClock::GetTimeToString().c_str());
                        });
        t.Add(data);

        std::string str = CClock::GetTimeToString();
        PrintBit(str.c_str(), str.size());
        exit(0);

        while (1)
        {
                CClock::UpdateTime();
                uint64_t now = CClock::GetTimeStamp();
                t.Update(now);
        }
        */

        return 0;
}
